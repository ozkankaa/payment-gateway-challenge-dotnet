name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  DOTNET_VERSION: "8.0.x"

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Start integration services
        run: |
          docker compose -f docker-compose.integration.yml up -d
          docker compose -f docker-compose.integration.yml ps

      - name: Wait for integration services
        run: |
          timeout 90 bash -c 'until nc -z localhost 8080; do sleep 2; done'
          timeout 90 bash -c 'until nc -z localhost 8086; do sleep 2; done'
          timeout 90 bash -c 'until nc -z localhost 5672; do sleep 2; done'
          timeout 90 bash -c 'until nc -z localhost 4318; do sleep 2; done'

      - name: Run tests
        env:
          ASPNETCORE_ENVIRONMENT: Testing
          RabbitMq__HostName: localhost
          RabbitMq__Port: "5672"
          RabbitMq__UserName: guest
          RabbitMq__Password: guest
          RabbitMq__VirtualHost: /
          AcquiringBank__BaseUrl: http://localhost:8080
          FraudService__BaseUrl: http://localhost:8086
          Service__OpenTelemetry__OtlpEndpoint: http://localhost:4318
          Serilog__WriteTo__1__Args__endpoint: http://localhost:4318/v1/logs
        run: dotnet test test/PaymentGateway.Api.Tests/PaymentGateway.Api.Tests.csproj --configuration Release --no-build --logger "console;verbosity=detailed"

      - name: Show docker logs on failure
        if: failure()
        run: docker compose -f docker-compose.integration.yml logs

      - name: Stop integration services
        if: always()
        run: docker compose -f docker-compose.integration.yml down -v